scripts:
  build:
    description: Build the docker image
    args:
    - name: tag
      required: true
    actions:
    - shell: docker build -f $plan/Dockerfile -t $(shuttle get docker.image):$tag .
  test:
    description: Run test for the project
    actions:
    - shell: go test